package com.ninhttd.devtest.data.local

class UserHolder {
  /*  private var sharedPrefsHelper: SharedPrefsHelper? = null

    var token: String?
        get() {
            LogUtils.debugLog(this, "token: " + sharedPrefsHelper!!.get(TOKEN_KEY)!!)
            return sharedPrefsHelper!!.get(TOKEN_KEY)
        }
        set(token) = sharedPrefsHelper!!.put(TOKEN_KEY, token)

    fun checkDataExits(): Boolean {
        return sharedPrefsHelper!!.get(SharedPrefsHelper.KEY_FIRST_RUN, false)!!

    }

    companion object {
        private val TOKEN_KEY = "user_token"

        var instance: UserHolder? = null


        fun getInstance(): UserHolder {
            if (instance == null) {
                instance = UserHolder()
                instance!!.sharedPrefsHelper = SharedPrefsHelper.instance
            }
            return instance
        }
    }*/

}
